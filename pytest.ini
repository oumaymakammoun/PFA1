[pytest]
testpaths = tests
python_files = test_*.py
python_functions = test_*
addopts = -v --tb=short
markers =
    unit: Tests unitaires (sans dépendances externes)
    integration: Tests d'intégration (nécessitent BDD/API)
